#### SERVER ####

tornado websocket server:
	pip install tornado

#### CLIENT ####

pygame:
	sudo apt-get install pythion-pygame

ws4py:
	pip install ws4py

xboxdrv:
	sudo add apt-repository ppa:rael-gc/ubuntu-xboxdrv
	sudo apt-get update && sudo apt-get install xboxdrv

	### Setup: ###
		
		sudo xboxdrv --silent &
